"use client";

// Legacy component no longer used.
// Checkout flow now handled by src/app/checkout/CheckoutClient.tsx
// Kept only as an empty placeholder to avoid build/type errors.

export default function CheckoutForm() {
  return null;
}
